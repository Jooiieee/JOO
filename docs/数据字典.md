# 电商数据库数据字典

> 数据库：ecommerce.db（sqlite3）｜共 7 张表 / 33 个字段 / 7 个主键 / 9 个外键 / 9 条关联关系

## 一、表与字段明细

| 表名 | 字段名称 | 字段类型 | 主键/外键 | 字段中文注释 |
|------|----------|----------|-----------|--------------|
| categories | id | INTEGER | PK | 主键ID |
| categories | name | VARCHAR(50) NOT NULL | | 分类名称 |
| users | id | INTEGER | PK | 主键ID |
| users | username | VARCHAR(50) NOT NULL | | 用户名 |
| users | email | VARCHAR(100) | | 邮箱 |
| users | created_at | TIMESTAMP DEFAULT CURRENT_TIMESTAMP | | 创建时间 |
| products | id | INTEGER | PK | 主键ID |
| products | name | VARCHAR(100) NOT NULL | | 商品名称 |
| products | category_id | INTEGER | FK→categories.id | 外键-分类ID |
| products | price | DECIMAL(10,2) | | 商品价格 |
| products | spec | VARCHAR(100) | | 商品规格 |
| addresses | id | INTEGER | PK | 主键ID |
| addresses | user_id | INTEGER | FK→users.id | 外键-用户ID |
| addresses | address | VARCHAR(255) | | 收货地址 |
| addresses | phone | VARCHAR(20) | | 联系电话 |
| orders | id | INTEGER | PK | 主键ID |
| orders | order_no | VARCHAR(50) UNIQUE NOT NULL | | 订单编号 |
| orders | user_id | INTEGER | FK→users.id | 外键-用户ID |
| orders | address_id | INTEGER | FK→addresses.id | 外键-收货地址ID |
| orders | total_amount | DECIMAL(10,2) | | 订单总金额 |
| orders | status | VARCHAR(20) DEFAULT 'completed' | | 订单状态 |
| orders | payment_method | VARCHAR(20) | | 支付方式（微信/支付宝/银行卡） |
| orders | created_at | TIMESTAMP DEFAULT CURRENT_TIMESTAMP | | 创建时间 |
| order_items | id | INTEGER | PK | 主键ID |
| order_items | order_id | INTEGER | FK→orders.id | 外键-订单ID |
| order_items | product_id | INTEGER | FK→products.id | 外键-商品ID |
| order_items | quantity | INTEGER | | 购买数量 |
| order_items | price | DECIMAL(10,2) | | 成交单价 |
| reviews | id | INTEGER | PK | 主键ID |
| reviews | user_id | INTEGER | FK→users.id | 外键-用户ID |
| reviews | product_id | INTEGER | FK→products.id | 外键-商品ID |
| reviews | order_id | INTEGER | FK→orders.id | 外键-订单ID |
| reviews | rating | INTEGER | | 评分 |
| reviews | comment | TEXT | | 评论内容 |
| reviews | created_at | TIMESTAMP DEFAULT CURRENT_TIMESTAMP | | 创建时间 |

## 二、外键关联关系

| 主表 | 从表 | 外键字段 | 关系类型 |
|------|------|----------|----------|
| categories | products | category_id | 1:N |
| users | addresses | user_id | 1:N |
| users | orders | user_id | 1:N |
| addresses | orders | address_id | 1:N |
| orders | order_items | order_id | 1:N |
| products | order_items | product_id | 1:N |
| users | reviews | user_id | 1:N |
| products | reviews | product_id | 1:N |
| orders | reviews | order_id | 1:N |

## 三、标识说明

- **PK**：Primary Key 主键
- **FK→表名.字段**：Foreign Key 外键，箭头指向被引用的主表字段
- **1:N**：一对多关系（主表一条记录对应从表多条记录）
