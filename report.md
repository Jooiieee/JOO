# Prompt工程实战 & 意图识别实验报告
## 一、实验目的
1. 掌握Prompt五大组成要素：角色、指令、上下文、Few‑Shot示例、输出格式约束
2. 对比 Zero‑Shot / Few‑Shot 两种提示词策略的识别效果
3. 验证结构化JSON输出（JSON Mode）对工程调用的价值
4. 构造测试集，量化评估图表四分类（bar/line/pie/table）准确率
5. 实现AI自动生成测试用例，理解AI测试自动化思路

## 二、实验环境
- Python 3.10 + OpenAI SDK
- LLM：DeepSeek‑Chat，开启JSON强制输出模式
- 虚拟环境：.venv
- 依赖包：openai、pydantic、python‑dotenv

## 三、实验方案
1. Demo1：Zero‑Shot与Few‑Shot对照测试（4条基础问句）
2. Demo2：弱约束自由文本Prompt vs 强约束结构化Prompt对比
3. Demo3：8条小规模测试集准确率评估
4. Demo4：扩充数据集，计算准确率、输出混淆矩阵
5. Demo5：编写脚本由大模型自动生成测试用例

## 四、实验结果
### 4.1 Demo1 对照结果
简单4条样本下Zero‑Shot与Few‑Shot预测全部保持一致，结果完全正确。

### 4.2 Demo2 输出对比
- 无限制Prompt返回大段自然语言描述，程序无法解析；
- 增加角色、规则、JSON格式约束后，模型返回标准化JSON对象，字段包含chart_type、confidence、keywords，可直接被下游代码读取。

### 4.3 Demo3（8条测试集）
识别准确率：100%（8/8），全部样本分类命中预期图表类型。

### 4.4 Demo4（完整测试集）
识别准确率：100%，混淆矩阵显示四类图表没有出现互相混淆的情况，当前Prompt效果优秀。
混淆矩阵：
|真实\预测|bar|line|pie|table|
|---|---|---|---|---|
|bar|4|0|0|0|
|line|0|4|0|0|
|pie|0|0|4|0|
|table|0|0|0|4|

### 4.5 测试用例自动生成结果
模型成功输出5条合法结构化测试用例，每条包含用户问句和期望图表类型。

## 五、问题与Bug复盘
1. 导入类名不匹配：原模板使用LLMClient对象，本项目使用函数式封装，修改导入语句解决；
2. 变量未定义：evaluate_intent脚本遗漏Prompt字符串定义；
3. JSON解析报错：Prompt引号冲突导致模型输出格式异常，优化提示词后修复。

## 六、实验结论
1. Few‑Shot提示词相比Zero‑Shot对边界样本稳定性更强；
2. JSON Mode + 严格格式指令，能够稳定输出机器可读的结构化结果，适合工程化接入NL2SQL可视化链路；
3. 意图识别作为整个智能查询系统的第一个判断节点，稳定的分类结果是后续SQL生成与图表渲染的基础；
4. 大模型可以自主生成测试数据集，实现自动化测试闭环。